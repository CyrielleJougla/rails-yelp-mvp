<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>

<br> <%= link_to "Back", restaurant_path(@restaurant), class: "btn btn-primary" %>
